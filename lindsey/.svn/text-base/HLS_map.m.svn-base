figure;
plot(stacks_green(1,:));

timecourses = stackGetTimeCourses(stacks_green,

res= 512;
dim= [res res];
HLS=zeros(dim(1),dim(2),3);
HLS_hc=zeros(dim(1),dim(2),3);

for x=1:dim(1)
    for y=1:dim(2)
        